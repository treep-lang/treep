def f(n: Int) returns: Int {
  match (n) {
    case 0 => { return 1 }
    case _ => { return n }
  }
}

def main() returns: Int {
  println("match_example input:")
  println(3)
  let result = f(3)
  println("match_example output:")
  println(result)
  return result
}