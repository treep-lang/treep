// Simple test macro to verify expansion works
macro testPrint {
  pattern: testPrint($msg)
  expand: {
    println(msg)
  }
}

def main() returns: Unit {
  println("=== simple macro test ===")
  testPrint("Hello from macro")
  println("=== done ===")
}
