const xs = [1, 2, 3]
const m  = { "a": 1, "b": 2 }

def demo() returns: Int {
<<<<<<< HEAD
  let s = 0
  s = s + xs.length()
  s = s + xs.head()
  s = s + xs.tail().length()
  s = s + m.size()
  for (p in: m.entries()) { s = s + snd(p) }
  return s
}

def main() returns: Int { return demo() }

=======
  println("standard_methods list xs:")
  println(xs)
  println("standard_methods dict m:")
  println(m)
  let s = 0
  let len = xs.length()
  println("xs.length():")
  println(len)
  s = s + len
  let head = xs.head()
  println("xs.head():")
  println(head)
  s = s + head
  let tailLen = xs.tail().length()
  println("xs.tail().length():")
  println(tailLen)
  s = s + tailLen
  let dictSize = m.size()
  println("m.size():")
  println(dictSize)
  s = s + dictSize
  println("m.entries():")
  for (p in: m.entries()) {
    println(p)
    s = s + snd(p)
  }
  return s
}

def main() returns: Int {
  let total = demo()
  println("standard_methods total:")
  println(total)
  return total
}
>>>>>>> aa17f4483079e0ab8e8dc740702d56c5122247d4
